{{ form_start(form, {'attr': {'class': 'app-form'}}) }}

<fieldset class="form-fieldset">
    <div class="form-group">
        <label for="{{ form.name.vars.id }}"
               class="form-label {% if form.name.vars.required %}required{% endif %}">
            {{ form.name.vars.label }}
        </label>
        {{ form_widget(form.name) }}
        {{ form_errors(form.name) }}
    </div>
</fieldset>

<fieldset class="form-fieldset">
    <legend class="form-legend">Membres du Groupe</legend>

    <div class="form-group">
        <label for="{{ form.members.vars.id }}"
               class="form-label {% if form.members.vars.required %}required{% endif %}">
            {{ form.members.vars.label }}
        </label>
        {{ form_widget(form.members) }}
        {{ form_errors(form.members) }}
    </div>

    <div class="form-group">
        <label for="{{ form.active.vars.id }}"
               class="form-label {% if form.active.vars.required %}required{% endif %}">
            {{ form_widget(form.active) }}
            {{ form.active.vars.label }}
        </label>
        {{ form_errors(form.active) }}
    </div>
</fieldset>

<button type="submit" class="app-btn cta">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
